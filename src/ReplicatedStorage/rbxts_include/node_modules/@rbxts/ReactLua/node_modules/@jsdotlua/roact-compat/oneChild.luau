-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

local v_u_1 = require(script.Parent.Parent.Parent:WaitForChild(".luau-aliases"):WaitForChild("@jsdotlua"):WaitForChild("react"))
local v_u_2 = require(script.Parent:WaitForChild("warnOnce"))
return function(p3) -- name: oneChild
	-- upvalues: (copy) v_u_2, (copy) v_u_1
	if _G.__COMPAT_WARNINGS__ then
		v_u_2("oneChild", "You likely don\'t need this at all! If you were assigning children via `React.oneChild(someChildren)`, you can simply use `someChildren` directly.")
	end
	if not p3 then
		return nil
	end
	local v4, v5 = next(p3)
	if not v5 then
		return nil
	end
	if next(p3, v4) then
		error("Expected at most one child, had more than one child.", 2)
	end
	return v_u_1.Children.only(v5)
end