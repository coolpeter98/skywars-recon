-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

local v1 = script.Parent.Parent.Parent
require(v1.ES7Types)
return function(p2)
	if p2 == nil then
		error("cannot extract values from a nil value")
	end
	local v3 = typeof(p2)
	local v4 = nil
	if v3 ~= "table" then
		if v3 == "string" then
			local v5 = p2:len()
			v4 = table.create(v5)
			for v6 = 1, v5 do
				v4[v6] = p2:sub(v6, v6)
			end
		end
		return v4
	end
	local v7 = {}
	for _, v8 in pairs(p2) do
		table.insert(v7, v8)
	end
	return v7
end