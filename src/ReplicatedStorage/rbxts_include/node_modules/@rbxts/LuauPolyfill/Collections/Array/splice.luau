-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

local v1 = script.Parent.Parent.Parent
require(v1.ES7Types)
return function(p2, p3, p4, ...)
	if #p2 < p3 then
		for v5 = 1, select("#", ...) do
			local v6 = select(v5, ...)
			table.insert(p2, v6)
		end
		return {}
	end
	local v7 = #p2
	if p3 < 1 then
		local v8 = v7 - math.abs(p3)
		p3 = math.max(v8, 1)
	end
	local v9 = {}
	local v10 = p4 or v7
	if v10 > 0 then
		local v11 = v10 - 1
		local v12 = p3 + math.max(0, v11)
		for _ = p3, math.min(v7, v12) do
			local v13 = table.remove(p2, p3)
			table.insert(v9, v13)
		end
	end
	for v14 = select("#", ...), 1, -1 do
		local v15 = select(v14, ...)
		table.insert(p2, p3, v15)
	end
	return v9
end