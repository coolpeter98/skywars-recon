-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

local v_u_1 = _G.__DEV__
local v2 = script.Parent
local v3 = v2.Parent.Parent
local v_u_4 = require(v2.isArray)
require(v3.ES7Types)
return function(p5, ...) -- name: concat
	-- upvalues: (copy) v_u_4, (copy) v_u_1
	local v6 = 0
	local v7, v8
	if v_u_4(p5) then
		v7 = table.clone(p5)
		v8 = #p5
	else
		v8 = v6 + 1
		v7 = {
			[v8] = p5
		}
	end
	for v9 = 1, select("#", ...) do
		local v10 = select(v9, ...)
		local v11 = typeof(v10)
		if v10 ~= nil then
			if v11 == "table" then
				if v_u_1 and not v_u_4(v10) then
					error("Array.concat(...) only works with array-like tables but it received an object-like table.\nYou can avoid this error by wrapping the object-like table into an array. Example: `concat({1, 2}, {a = true})` should be `concat({1, 2}, { {a = true} }`")
				end
				for v12 = 1, #v10 do
					v8 = v8 + 1
					v7[v8] = v10[v12]
				end
			else
				v8 = v8 + 1
				v7[v8] = v10
			end
		end
	end
	return v7
end