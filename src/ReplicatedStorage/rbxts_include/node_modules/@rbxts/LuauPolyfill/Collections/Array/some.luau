-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

local v1 = script.Parent.Parent.Parent
require(v1.ES7Types)
return function(p2, p3, p4)
	if typeof(p2) ~= "table" then
		error(string.format("Array.some called on %s", (typeof(p2))))
	end
	if typeof(p3) ~= "function" then
		error("callback is not a function")
	end
	for v5, v6 in p2 do
		if p4 == nil then
			if v6 ~= nil and p3(v6, v5, p2) then
				return true
			end
		elseif v6 ~= nil and p3(p4, v6, v5, p2) then
			return true
		end
	end
	return false
end