-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

return function(p1, ...) -- name: assign
	local v2 = type(p1) == "table"
	local v3 = ("Expected a table for first argument, got $%*"):format((type(p1)))
	assert(v2, v3)
	for v4 = 1, select("#", ...) do
		for v5, v6 in select(v4, ...) do
			p1[v5] = v6
		end
	end
	return p1
end