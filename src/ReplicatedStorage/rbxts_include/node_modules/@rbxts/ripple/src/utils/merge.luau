-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

return function(p1, p2) -- name: merge
	local v3 = table.clone(p1)
	for v4, v5 in pairs(p2) do
		v3[v4] = v5
	end
	return v3
end